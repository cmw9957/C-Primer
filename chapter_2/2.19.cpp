//포인터와 참조자 사이에 핵심적인 차이를 설명한다.

//포인터는 선언과 동시에 반드시 초기화를 해주지 않아도 되고 가리키는 객체를 바꿀수 있다. 
//참조자는 포인터와 달리 선언과 동시에 반드시 초기화를 해주어야하고 처음 결합한 객체 외에 다른 객체와 결합을 하지 못한다.

//포인터는 객체이지만 참조자는 객체가 아니다. 

//포인터는 객체의 주소를 값으로 가지고 있고 참조자는 객체의 별명으로서 존재한다. 
